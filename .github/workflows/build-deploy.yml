name: Build and Deploy Code

on:
  push:
    branches:
      - "main"

jobs:
  job1:
    runs-on windows-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v7
      - name: say hi to simon
        run: echo "hello simon"